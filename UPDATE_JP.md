# 更新履歴 (Updates)

本ドキュメントでは、元のリポジトリから追加・改善された主要な機能について説明します。

## 0. 環境構築 (Environment Setup)

スパコンやクラスタ環境（Cray XD2000等）で環境を構築する場合、同梱の `setup.sh` を使用してください。このスクリプトは、`mpi4py` をシステムのMPIライブラリと正しくリンクさせるためのビルド処理を自動的に行います。

```bash
# 1. ログインノードでセットアップスクリプトを実行
bash setup.sh

# 2. 作成された仮想環境をアクティベート
source .venv/bin/activate

# 3. 実行用バッチファイル（例: job.sh）をジョブスケジューラに登録
qsub job.sh
```

## 1. MPI 分散処理への対応 (MPI Distributed Execution)

大規模なデータセットや計算コストの高い多数の生成モデルを効率的に評価するため、`mpi4py` を用いた **MPI (Message Passing Interface) による分散並列実行** をサポートしました。

### 主な特徴
- **マルチノード・マルチコアスケール**: `joblib` による単一マシンの並列化の限界を超え、複数ノードのクラスタ全体にタスクを分散させることが可能です。
- **階層的並列化 (MPI + Joblib)**: Rank 0がMPIノード間でタスクチャンクを動的に分散し、各ノード内では `joblib` (`loky` バックエンド) を用いてローカル並列処理を実行します。これにより、大規模なデータフレームのメモリ共有が効率化され、プロセス間通信のオーバーヘッドが最小限に抑えられます。
- **分散評価エンジン**: タスク配布を調整する堅牢なフレームワーク（`EvaluationEngine`）。
- **階層型並列化**: ノード間通信に MPI を、ノード内並列化に `joblib` を組み合わせた効率的な実行。
- **タスク・フラット化（パフォーマンス最適化）**: 評価タスクを全イテレーションにわたってフラット化。全ノードが全モデル・全イテレーションのペアを同時に処理でき、スパコンでのスループットを最大化します。
- **デュアルモード実行**:
    - **MPIモード**: `mpirun` で実行された場合に自動的に有効化。
    - **ローカルモード**: ローカルマシンでは標準のマルチプロセス処理にフォールバックし、完全な後方互換性を維持。
- **統合的なCLIランク管理**: 一元化された `EvaluationEngine` により、ディレクトリの作成、ログ出力、結果の集約、データのブロードキャスト (`bcast`) などのI/O処理をRank 0のみが排他的に実行します。ワーカーランク (`rank > 0`) は冗長なログ出力をスキップし、ファイルの競合を防ぎます。
- **マスター・ワーカー型のタスク分散**: 対称的なSPMDモデルではなく、マスター・ワーカー方式を採用しています。Rank 0がマスターとして機能し、ワーカーノード (Rank > 0) に対してタスクチャンクを動的に割り当てることで負荷分散を行います。同時に、GPUを使用するアルゴリズムに対してはCUDAメモリ不足を回避するために適切なシリアル実行を維持します。
- **安全なフォールバック機構**: OS側にMPIライブラリ（`libmpi.so`）が存在しない環境でも、内部で `RuntimeError` を適切にキャッチし、自動的に標準の `joblib` ベースの並列処理にフォールバックする後方互換性を備えています。

### 使用方法

MPIランチャー (`mpirun`) によるノードレベルの分散と、`--workers` (`-W`) フラグによるコアレベルのローカル並列化を組み合わせて実行します。

**例: スパコンやクラスタ環境での実行**
PBS等のジョブスケジューラを使用する場合、各ノードに1つのMPIプロセスを割り当て (`mpiprocs=1`)、ノード内の全コアを `--workers` で利用する構成が一般的です。

```bash
# ジョブスクリプトのリソース確保例 (例: 10ノード、各192コア、ノードあたり1 MPIプロセス)
# #PBS -l select=10:ncpus=192:mem=740gb:mpiprocs=1

# 実行コマンド
mpirun ./.venv/bin/python all_cli.py \
    -D data/dataset_name \
    -O outputs/dataset_name \
    -RCU tests/utility/runconfig.json \
    -RCL tests/linkage/runconfig.json \
    -RCI tests/inference/runconfig.json \
    -W 192 \
    --device cpu \
    --use-mpi
```

## 2. 堅牢な実行キャッシュへの対応 (Robust Execution Caching)

複雑な生成モデルの評価には数日かかることがあります。途中でプロセスが終了した場合のデータ損失を防ぎ、反復的な実験を高速化するために、**堅牢なキャッシュシステム**が導入されました。

### 主な特徴
- **自動保存とレジューム**: 各モデルの評価結果は自動的にキャッシュディレクトリに保存されます。`Ctrl+C` やノードの障害などで途中終了した場合でも、再実行時には完了済みのタスクがキャッシュから瞬時に読み込まれ、未完了のタスクから評価が再開されます。
- **安全なアトミック書き込み**: キャッシュの保存は一時ファイル (`.tmp.PID`) への書き込みと `os.replace` によるアトミックなリネームによって行われます。これにより、保存中にプロセスが強制終了されても、破損した不完全なキャッシュファイルが生成されることはありません。
- **ハッシュによる自動キャッシュ無効化**:
  キャッシュファイルの識別キー（ファイル名）には、モデル名やイテレーション数に加えて、**モデルのハイパーパラメータとデータセット名を組み合わせたMD5ハッシュ値**が含まれます。
  これにより、`runconfig.json` でパラメータを一部変更した場合（例: `epsilon` を 1.0 から 10.0 に変更）、ハッシュ値が変化するため、誤って古いキャッシュを読み込むことなく自動的に再計算が実行されます。
- **厳密な再現性の保証**:
  GPU実行時などメインプロセスでタスクがシリアルに評価される際、モデル内部での乱数消費がメインプロセスの乱数状態（`numpy.random` など）に影響を与えないよう、評価の直前・直後で乱数状態を退避・復元する仕組みを導入しました。これにより、キャッシュヒット時と通常実行時の後続のデータサンプリングの乱数シードが完全に一致し、厳密な再現性が保証されます。
