# 更新履歴 (Updates)

本ドキュメントでは、元のリポジトリから追加・改善された主要な機能について説明します。

## 1. MPI 分散処理への対応 (MPI Distributed Execution)

大規模なデータセットや計算コストの高い多数の生成モデルを効率的に評価するため、`mpi4py` を用いた **MPI (Message Passing Interface) による分散並列実行** をサポートしました。

### 主な特徴
- **マルチノード・マルチコアスケール**: `joblib` による単一マシンの並列化の限界を超え、複数ノードのクラスタ全体にタスクを分散させることが可能です。
- **動的ワーカー割り当て**: タスクを各MPIランクに自動的に分割（SPMDモデル）しつつ、GPUを使用するアルゴリズムに対してはCUDAメモリ不足を回避するために適切なシリアル実行を維持します。
- **安全なフォールバック機構**: OS側にMPIライブラリ（`libmpi.so`）が存在しない環境でも、内部で `RuntimeError` を適切にキャッチし、自動的に標準の `joblib` ベースの並列処理にフォールバックする後方互換性を備えています。

### 使用方法
CLI実行時に `--use-mpi` フラグを付与することでMPIを有効化できます。
```bash
mpirun -np 4 uv run python all_cli.py -D data/adult --use-mpi
```

## 2. 堅牢な実行キャッシュへの対応 (Robust Execution Caching)

複雑な生成モデルの評価には数日かかることがあります。途中でプロセスが終了した場合のデータ損失を防ぎ、反復的な実験を高速化するために、**堅牢なキャッシュシステム**が導入されました。

### 主な特徴
- **自動保存とレジューム**: 各モデルの評価結果は自動的にキャッシュディレクトリに保存されます。`Ctrl+C` やノードの障害などで途中終了した場合でも、再実行時には完了済みのタスクがキャッシュから瞬時に読み込まれ、未完了のタスクから評価が再開されます。
- **安全なアトミック書き込み**: キャッシュの保存は一時ファイル (`.tmp.PID`) への書き込みと `os.replace` によるアトミックなリネームによって行われます。これにより、保存中にプロセスが強制終了されても、破損した不完全なキャッシュファイルが生成されることはありません。
- **ハッシュによる自動キャッシュ無効化**:
  キャッシュファイルの識別キー（ファイル名）には、モデル名やイテレーション数に加えて、**モデルのハイパーパラメータとデータセット名を組み合わせたMD5ハッシュ値**が含まれます。
  これにより、`runconfig.json` でパラメータを一部変更した場合（例: `epsilon` を 1.0 から 10.0 に変更）、ハッシュ値が変化するため、誤って古いキャッシュを読み込むことなく自動的に再計算が実行されます。
- **厳密な再現性の保証**:
  GPU実行時などメインプロセスでタスクがシリアルに評価される際、モデル内部での乱数消費がメインプロセスの乱数状態（`numpy.random` など）に影響を与えないよう、評価の直前・直後で乱数状態を退避・復元する仕組みを導入しました。これにより、キャッシュヒット時と通常実行時の後続のデータサンプリングの乱数シードが完全に一致し、厳密な再現性が保証されます。
